# Event-engine 推送复盘：2026-05-04

## 窗口

- 本地日期：2026-05-04 00:00:00 到 2026-05-05 00:00:00（Asia/Shanghai）
- UTC 窗口：2026-05-03 16:00:00 到 2026-05-04 16:00:00

## Actor 汇总

本窗口内没有 actor 满足有效推送条件：`delivery_log.status IN ('sent','dryrun')` 且 `channel IN ('sink','digest')`。

补充检查：同一窗口内 `delivery_log` 总行数为 0，因此也没有可审计的 `digest_item`、`global_digest_item`、`prefs`、`router`、`omitted`、`filtered`、`no_actor` 或 `failed` 证据。

## 关键发现

- `duplicate`：无有效推送，未发现重复推送。
- `noise`：无有效推送，未发现噪音推送。
- `bad_timing`：无有效推送，未发现推送时间问题。
- `should_filter`：无有效推送，未发现应过滤但已发送的内容。
- `should_digest`：无相关 omitted/filtered/no_actor/failed 记录可判断。
- `should_immediate`：无相关 omitted/filtered/no_actor/failed 记录可判断。
- `baseline_candidate`：无。

## Representative good pushes

无。

## 建议后续动作

无需代码、偏好或 baseline 调整。若 2026-05-04 原本预期存在推送，请优先检查 event-engine 当日采集、调度或写库任务是否运行。

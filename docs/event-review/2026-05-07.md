# Event-engine 推送复盘：2026-05-07

## 窗口

- 本地日期：2026-05-07 00:00:00 到 2026-05-08 00:00:00（Asia/Shanghai）
- UTC 窗口：2026-05-06 16:00:00 到 2026-05-07 16:00:00

## Actor 汇总

有效推送条件：`delivery_log.status IN ('sent','dryrun')` 且 `channel IN ('sink','digest')`。

| actor | immediate sent | digest batches | digest items sent / omitted | filtered / no_actor / failed | 其它证据 |
| --- | ---: | ---: | ---: | ---: | --- |
| `telegram::::6996473277` | 4 | 1 | 1 / 1 | 397 / 0 / 0 | queued 3；portfolio-only，持仓 `VST` |
| `telegram::::8039067465` | 19 | 2 | 24 / 397 | 0 / 0 / 0 | queued 442；capped 16；cooled_down 3 |

全局 router 证据：`event_engine::::no_actor` 在窗口内有 `no_actor` 5649 条，其中 high `news_critical` 186 条、medium 1703 条。窗口内未见 `global_digest_item` 与 `failed` 记录。

## 关键发现

- `duplicate`：未发现同一 actor 下相同 `event_id` 或相同标题被重复发送到 `sink` / `digest_item`。
- `duplicate` / `noise` / `should_digest` / `baseline_candidate`：`telegram::::8039067465` 在 2026-05-07 11:12:51-11:12:54 连续收到 7 条 AMD `analyst_grade` sink 推送，全部来自同一 URL `https://thefly.com/ajax/news_get.php?id=4346982`。代表事件：`grade:AMD:2026-05-06T13:38:53.000Z:Needham`、`...:Raymond James`、`...:Wolfe Research`、`...:Citigroup`、`...:Jefferies`、`...:Oppenheimer`、`...:BTIG`。这些是不同券商动作，但同源同批次在 4 秒内逐条 immediate，体验上像评级噪音；建议后续把同 URL / 同 ticker / 同批 analyst fanout 聚合成一条 sink 或降入 digest。
- `should_digest` / `baseline_candidate`：`event_engine::::no_actor` 在 2026-05-07 18:51:58 记录 high `news_critical`：`news:https://www.bloomberg.com/news/articles/2026-05-07/coreweave-s-stunning-rally-creates-prove-it-moment-for-earnings`，标题 `CoreWeave’s Stunning Rally Creates Prove-It Moment for Earnings`，URL `https://www.bloomberg.com/news/articles/2026-05-07/coreweave-s-stunning-rally-creates-prove-it-moment-for-earnings`。`telegram::::8039067465` 持仓含 `CRWV`，但该事件 `symbols_json=[]`，未进入 19:00 digest；建议补一类公司名到 ticker 的实体链接 baseline。
- `bad_timing`：`telegram::::8039067465` 的 digest 在 12:00:32 与 19:01:04 发送，贴近配置的 `12:00` / `19:00` slot；sink 推送未落在 00:00-08:00 quiet hours。`telegram::::6996473277` 未配置 timezone/digest_slots，19:01 的单条 digest 未见明显时机错误。
- `should_filter`：未发现明显应过滤却发送的 class-action、deadline、PR-wire 或无关低价值内容。`telegram::::6996473277` 的 `COIN` WatcherGuru 社交帖被 omitted，符合其 portfolio-only `VST` 偏好。
- `should_immediate`：`telegram::::8039067465` 的 high `sec_filing` 10-Q 事件 `sec:AMD:...amd-20260328.htm`、`sec:COHR:...iivi-20260331.htm`、`sec:TEM:...tem-20260331.htm` 在 11:13 被 `cooled_down`，随后 12:00 digest 送达，未判定为漏发 immediate。

## Representative good pushes

- `telegram::::6996473277`，2026-05-07 19:59:07，sent/sink，`sec:VST:https://www.sec.gov/Archives/edgar/data/1692819/000169281926000011/vistra-20260331xearningsre.htm`，`VST filed 8-K`，直接命中唯一持仓并带 SEC URL。
- `telegram::::6996473277`，2026-05-07 20:28:19，sent/sink，`news:https://www.reuters.com/business/energy/vistra-swings-quarterly-profit-rising-power-demand-2026-05-07/`，`Vistra swings to quarterly profit on rising power demand`，与 VST 主线相关且来源质量高。
- `telegram::::8039067465`，2026-05-07 11:12:50，sent/sink，`earnings_surprise:AMD:2026-05-05`，`AMD 财报 超预期 +6.2%`，包含 EPS actual / expected 与来源 URL。
- `telegram::::8039067465`，2026-05-07 12:00:32，sent/digest_item，`price_close:AMD:2026-05-06`，`AMD +18.61%`，作为午间 digest 的高优先级收盘异动合理。
- `telegram::::8039067465`，2026-05-07 21:43:18，sent/sink，`price_band:AAOI:2026-05-07:down:600`，`AAOI 跨过 -6% 档`，命中持仓价格阈值，且未在 quiet hours。

## 建议后续动作

- 代码修复候选：对 analyst-grade 同源同 ticker fanout 做聚合或更严格的 immediate cap，优先避免数秒内多条同 URL sink。
- Baseline 候选：加入 CoreWeave/CRWV 公司名实体链接样本，验证 high RSS news 能进入相关 actor 的 digest 候选。
- 本次仅产出日审报告，不修改代码、fixture、偏好或动态计划。

# Event-engine 推送复盘：2026-05-21

## 窗口

- 本地日期：2026-05-21 00:00:00 到 2026-05-22 00:00:00（Asia/Shanghai）
- UTC 窗口：2026-05-20 16:00:00 到 2026-05-21 16:00:00
- 有效推送条件：`delivery_log.status IN ('sent','dryrun')` 且 `channel IN ('sink','digest')`
- 只读 exporter：未运行；本窗口内没有符合条件的 actor

## Actor 汇总

| actor | immediate sent | digest batches | digest items sent / omitted | filtered / no_actor / failed | 其它证据 |
| --- | ---: | ---: | ---: | ---: | --- |
| 无 | 0 | 0 | 0 / 0 | 0 / 0 / 0 | `delivery_log` 在本窗口内无记录 |

## 关键发现

- `duplicate`：无有效推送，未发现重复 user-visible 推送。
- `noise` / `should_filter`：无有效推送，未发现应过滤却发出的内容。
- `bad_timing` / `should_digest` / `should_immediate`：无有效推送，也没有可用于判断的 digest batch、filtered、omitted、capped、cooled_down、no_actor 或 failed 证据。
- `baseline_candidate`：无；本窗口没有可复用的路由或分类样本。

## Representative good pushes

- 无。本窗口没有 `sink` 或 `digest` 的 `sent` / `dryrun` 推送。

## 建议后续动作

- No action：本次只提交无有效推送报告，不修改代码、fixture、偏好、SQLite 数据或动态计划。
- Data watch：本地 `data/events.sqlite3` 最新 delivery 记录停在 2026-05-20 10:48:37（Asia/Shanghai）；后续复盘如持续无记录，应检查 event-engine 数据采集或同步是否中断。

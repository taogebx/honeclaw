# Event-engine 推送复盘：2026-05-16

## 窗口

- 本地日期：2026-05-16 00:00:00 到 2026-05-17 00:00:00（Asia/Shanghai）
- UTC 窗口：2026-05-15 16:00:00 到 2026-05-16 16:00:00

## Actor 汇总

有效推送条件：`delivery_log.status IN ('sent','dryrun')` 且 `channel IN ('sink','digest')`。

本窗口内没有任何 actor 有有效推送，因此未运行 per-actor exporter。

补充检查：同一窗口内 `delivery_log` 总行数为 0，因此也没有可审计的 `digest_item`、`global_digest_item`、`prefs`、`router`、`omitted`、`filtered`、`no_actor` 或 `failed` 证据。当前本地 `data/events.sqlite3` 的最新 `delivery_log` 记录停在 2026-05-15 15:17:10（Asia/Shanghai）。

## 关键发现

- `duplicate`：无有效推送，未发现重复推送。
- `noise`：无有效推送，未发现噪音推送。
- `bad_timing`：无有效推送，未发现推送时间问题。
- `should_filter`：无有效推送，未发现应过滤但已发送的内容。
- `should_digest`：无相关 omitted/filtered/no_actor/failed 记录可判断。
- `should_immediate`：无相关 omitted/filtered/no_actor/failed 记录可判断。
- `baseline_candidate`：无。

## Representative good pushes

无。

## 建议后续动作

- No action：本日没有 sent/dryrun 推送可判定质量。
- 观察项：若 2026-05-16 原本预期存在推送，请优先检查 event-engine 采集、调度或写库任务是否在 2026-05-15 15:17:10（Asia/Shanghai）之后继续运行。
- 本次仅产出日审报告，不修改代码、fixture、偏好或动态计划。
